# Packaging and dependencies

Only MIT skill/adoption content included; no BSL engine, binaries, gateway, MCP server, browser runtime or memory core is redistributed.

Compression requires Claude CLI or the optional anthropic Python SDK with user credentials; benchmark tiktoken is optional. No provider calls made.
