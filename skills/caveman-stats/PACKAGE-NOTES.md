# Packaging and dependencies

Only MIT skill/adoption content included; no BSL engine, binaries, gateway, MCP server, browser runtime or memory core is redistributed.

Claude token report requires explicit host hook configuration and existing local transcripts; hooks are bundled but not installed or executed. Other hosts use their native report or honestly report unavailable usage.
